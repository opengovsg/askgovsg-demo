doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title post.pug
  body
    h1 post table
    ul
      each row in results
        li= JSON.stringify(row)
